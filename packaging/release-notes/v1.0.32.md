## Changed

- **A new installer for Windows.** Same choices as before — install for
  everyone or just you, and it remembers where you put it — in a window that
  belongs to the application rather than to the toolkit that built it. Three
  pages: where it goes, a progress card while it copies, and done.

Nothing else changed. Updating from inside the application works as usual, and
so does the uninstaller.
