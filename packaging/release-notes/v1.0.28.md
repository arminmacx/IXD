## Fixed

- **A download from the browser can reach the front again.** With Settings, the
  Log, the update window or any other dialog open, the window asking where to
  put a download could not be brought forward — the dialog was holding the
  whole application. Each one now blocks only the window it was opened from.
