## Fixed

- **The Windows installer's text is readable again.** Punctuation on the first
  page and in its dialogs came out as two characters instead of one — `Â·`
  where a separator belonged. The installer was written as UTF-8 and compiled
  as if it were not.
