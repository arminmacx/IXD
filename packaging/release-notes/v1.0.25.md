## Fixed

- **The file-info window no longer disappears when you click away.** Clicking
  back to the browser took it off the taskbar, and returning to the application
  brought back only the main window — the window asking where to save the file
  was gone. It is now a window in its own right rather than one owned by the
  main window, so it keeps its own taskbar button and stays where you left it.
